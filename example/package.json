{
  "name": "midway-react-faas",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run build:client && npm run build:server",
    "build:client": "tsc && vite build",
    "build:server": "mw build",
    "deploy": "npm run build && mw deploy",
    "serve": "vite preview",
    "test": "jest"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/koa-router": "^7.4.1",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.1.0",
    "jest": "^26.6.3",
    "typescript": "^4.2.2",
    "vite": "^2.0.1"
  }
}
